module.exports = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
    // locales: process.env.NEXT_PUBLIC_NEW_SIGNUP_FLOW //we don't need it until we will have translations
    //   ? ['en', 'es', 'sv', 'no']
    //   : ['en'],
    localeDetection: false,
  },
}
